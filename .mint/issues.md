# Mint Issues & Learnings

_Centralized log. All agent blockers, root causes, and learnings go here._

| Date       | Task                 | Severity | Issue                                                                                                                                                                                                                                                      | Root Cause                                                                                                                                                                                            | Resolution                                                                                                                                                                                                                | Spec Fix                                                                                                                                                      |
| ---------- | -------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-03-19 | Locale system debug  | CRITICAL | 3 conflicting sources of truth: Nitro plugin hardcoded 'en', i18n `defaultLocale: 'en'`, and `detectBrowserLanguage` overwriting cookies. Symptoms: 0 products returned, wrong translations, raw i18n keys rendered, locale switcher showing wrong locale. | `detectBrowserLanguage` fought our custom locale cookie management. Short locale codes ('sv') passed to GraphQL returned 0 results — GraphQL needs BCP-47 ('sv-SE').                                  | Disabled `detectBrowserLanguage`, changed `defaultLocale` to 'sv', added `ensureBcp47Locale` as API gatekeeper, added `SupportedLocale` type to lock down locale strings.                                                 | Hard-blocks added for locale rules. Architecture documented in `docs/conventions/ssr.md`.                                                                     |
| 2026-03-17 | SSR hard-refresh fix | CRITICAL | ProductList.vue crashes with "Cannot convert undefined or null to object" during SSR render, causing 500 errors on hard refresh for category pages (/foder, /kompressorer).                                                                                | Template and script access async data (useFetch results, route.query) without null guards. SSR renders templates before async data resolves. Also `window.scrollTo` called without browser API guard. | Added null guards (optional chaining + nullish coalescing) to ProductList.vue, search.vue, OrderConfirmation.vue. Guarded window.scrollTo with import.meta.client check. Added ProductList.test.ts with SSR safety tests. | Added hard block + SSR convention doc section + instinct pattern (confidence 5) to prevent recurrence.                                                        |
| 2026-03-09 | SAL-22               | HIGH     | Reviewers and documenters never ran during implementation. 5 quality + 3 convention warnings shipped uncaught.                                                                                                                                             | Orchestrator dispatched a single planner to "decompose and execute all specs" instead of running the full pipeline per spec. Spec review, stage 2 audit, and documenter triggers were skipped.        | Required a second pass (7 more commits) to fix type casts, DRY violations, missing data-testid, stale docs.                                                                                                               | **NEVER dispatch planner to "do everything". Orchestrate per-spec: implement → spec review → stage 2 audit → documenter. Each stage is a separate dispatch.** |
| 2026-03-09 | SAL-22               | MEDIUM   | `as any` / `as Record<string, unknown>` casts (6 occurrences) to access SDK fields missing from types.                                                                                                                                                     | SDK types lack fields that GraphQL fragments return. Planner used casts instead of extending types.                                                                                                   | Created `DetailProduct` and `CampaignInfo` in `shared/types/commerce.ts`. Removed all casts.                                                                                                                              | **When SDK types miss fields, extend in shared/types/commerce.ts. Never `as any` for known fields.**                                                          |
| 2026-03-09 | SAL-22               | MEDIUM   | Identical computeds (~20 lines) duplicated in CartDrawer + CartPage. Campaign filter duplicated in 5 components.                                                                                                                                           | Planner implemented each component independently without cross-component DRY check.                                                                                                                   | Extracted to cart store getters + `filterVisibleCampaigns()` utility.                                                                                                                                                     | **Before adding logic to a second component, check if it exists elsewhere. Extract to store/composable/utility.**                                             |
