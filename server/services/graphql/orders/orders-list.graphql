query getCompanyOrders($channelId: String, $languageId: String, $marketId: String) {
  getCompanyOrders(channelId: $channelId, languageId: $languageId, marketId: $marketId) {
    id
    publicId
    status
    createdAt
    customerId
    customerEmail
    billingAddress {
      firstName
      lastName
      company
    }
    cart {
      summary {
        total {
          sellingPriceIncVat
          sellingPriceIncVatFormatted
        }
      }
    }
  }
}
