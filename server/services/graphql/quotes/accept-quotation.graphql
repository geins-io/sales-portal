mutation acceptQuotation($quotationId: Guid!, $languageId: String) {
  acceptQuotation(quotationId: $quotationId, languageId: $languageId) {
    id
    items {
      ...QuotationCartItem
    }
    summary {
      ...QuotationCartSummary
    }
    quotation {
      ...QuotationCore
    }
  }
}
