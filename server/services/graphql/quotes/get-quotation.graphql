query getQuotationCart($quotationId: Int!, $channelId: String, $languageId: String, $marketId: String) {
  getQuotationCart(quotationId: $quotationId, channelId: $channelId, languageId: $languageId, marketId: $marketId) {
    id
    items {
      ...QuotationCartItem
    }
    summary {
      ...QuotationCartSummary
    }
    quotation {
      ...QuotationWithAddresses
    }
  }
}
