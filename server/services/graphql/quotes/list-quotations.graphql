query listQuotationCarts($channelId: String, $languageId: String, $marketId: String) {
  listQuotationCarts(channelId: $channelId, languageId: $languageId, marketId: $marketId) {
    id
    items {
      ...QuotationCartItem
    }
    summary {
      ...QuotationCartSummary
    }
    quotation {
      ...QuotationWithAddresses
    }
  }
}
