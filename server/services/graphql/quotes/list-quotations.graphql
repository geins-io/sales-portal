query listQuotationCarts($languageId: String) {
  listQuotationCarts(languageId: $languageId) {
    id
    items {
      ...QuotationCartItem
    }
    summary {
      ...QuotationCartSummary
    }
    quotation {
      ...QuotationWithAddresses
    }
  }
}
