mutation rejectQuotation($quotationId: Int!, $channelId: String, $languageId: String, $marketId: String) {
  rejectQuotation(quotationId: $quotationId, channelId: $channelId, languageId: $languageId, marketId: $marketId) {
    id
    items {
      ...QuotationCartItem
    }
    summary {
      ...QuotationCartSummary
    }
    quotation {
      ...QuotationCore
    }
  }
}
